"CREATE git"
git init - новый репозиторий
git status - статус git

"COMMIT"
git add . - затрекать все изменения
git commit -m "Текст" - Коммит файлов и добавление названия коммита
git checkout -b develop - создание ветки разработки
git checkout -b features/cc-100-helloworld - создание ветки функций и функции cc-100-helloworld

"Reset"
git reset хэш-номер коммита - откатиться к нужной версии

"Merge"
git checkout develop -Перейти на ветку develop
git merge features/cc-100-helloworld - Имя ветки с которой нужно слить в ветку develop
